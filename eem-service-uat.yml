spring:
  datasource:
    url: jdbc:mysql://localhost:3307/employee_db
    username: root
    password: as_ENV_var

eem:
  scheduler:
    cron: "0 */1 * * * *"
    enabled: false

logging:
  level:
    org.springframework.cloud.context.scope.refresh: DEBUG
